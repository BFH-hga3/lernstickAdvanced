disable_timeout

function export_keyboard {
	set KEYBOARD="${1}"
	export KEYBOARD
	go_to_main_menu
}

menuentry $"Select keyboard:" --unrestricted {
	go_to_main_menu
}

entry_text=$"Switzerland (German)"
if [ "${KEYBOARD}" = "ch(de),ch(fr),de,fr" ]
then
	menuentry "* ${entry_text}" --unrestricted {
		go_to_main_menu
	}
else
	menuentry "  ${entry_text}" --unrestricted {
		export_keyboard "ch(de),ch(fr),de,fr"
	}
fi

entry_text=$"Switzerland (French)"
if [ "${KEYBOARD}" = "ch(fr),ch(de),fr,de" ]
then
	menuentry "* ${entry_text}" --unrestricted {
		go_to_main_menu
	}
else
	menuentry "  ${entry_text}" --unrestricted {
		export_keyboard "ch(fr),ch(de),fr,de"
	}
fi

entry_text=$"Italy"
if [ "${KEYBOARD}" = "it,ch(de),ch(fr),fr" ]
then
	menuentry "* ${entry_text}" --unrestricted {
		go_to_main_menu
	}
else
	menuentry "  ${entry_text}" --unrestricted {
		export_keyboard "it,ch(de),ch(fr),fr"
	}
fi

