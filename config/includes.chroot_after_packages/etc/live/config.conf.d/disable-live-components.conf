# Disable live-config components that conflict with MSE exam setup.
# sudo:      0040-sudo grants live user NOPASSWD — replaced by 88-mse_setup_users
# policykit: not needed in exam environment
LIVE_CONFIG_NOCOMPONENTS="${LIVE_CONFIG_NOCOMPONENTS},sudo,policykit"
